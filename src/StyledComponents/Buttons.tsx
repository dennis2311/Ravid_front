import styled from "styled-components";

export const StylelessButton = styled.button`
    color: inherit;
    background: none;
    padding: 0;
    border: none;
    outline: none;
    cursor: pointer;
`;
