import { Color } from "./Values";
import { HeaderTheme, FooterTheme } from "./Components";
import { HomeTheme, SynthesisTheme } from "./Page";

const Theme = {
    color: Color,
    // Components
    header: HeaderTheme,
    footer: FooterTheme,
    // Page
    synthesis: SynthesisTheme,
    home: HomeTheme,
};

export default Theme;
